name: quiet_material
description: Private Quiet Material component catalog, theme, adaptive shell, and motion helpers for Flutter.
version: 1.3.0
publish_to: none
environment:
  sdk: '>=3.9.0 <4.0.0'
  flutter: '>=3.35.0'
dependencies:
  flutter:
    sdk: flutter
  animations: 2.2.0
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
